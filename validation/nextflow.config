// Configuration for validation pipeline
params {
    outdir = 'results'
}

// Configure the codespaces executor
executor {
    name = 'codespaces'
}

// Process configuration
process {
    executor = 'codespaces'
}
